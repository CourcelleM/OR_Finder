__default__:
        job-name: "{rule}"
        partition: "agap"
        queue: "agap_normal"
        nodes: "--nodes=1 "
        cpus: "--cpus-per-task=1"
        time: "--time=1:00:00"
        out: "--output={params.clust_log}"
        mem: ""

Hmmer:
        cpus : "--cpus-per-task=2"

Extract_hmmout:
        mem: "--mem 10G"

ORA:
        time: "--time=2:00:00"
        mem: "--mem 10G"

Phylofilter:
        cpus: "--cpus-per-task=2"
        mem: "--mem 10G"
